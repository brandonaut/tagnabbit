## 1. Speed control — remove dropdown

- [x] 1.1 In `src/PlayerPage.tsx`, delete the `<select>` from the speed control block; keep the `Minus` / `Plus` stepper buttons and their `disabled` / `getAdjacentSpeed` logic unchanged.
- [x] 1.2 Between the steppers, render the current speed as `<span className="w-12 text-center tabular-nums">{speed}x</span>`.
- [x] 1.3 Confirm `handleSpeedChange`, `handleSpeedStep`, `getAdjacentSpeed`, and `SPEED_OPTIONS` still compile and are still referenced; remove nothing else.

## 2. Minimap scrubber — markup and drawing

- [x] 2.1 Add refs for the minimap wrapper, minimap canvas, minimap playhead, and minimap window box; add a `minimapWidthRef` for the wrapper's client width.
- [x] 2.2 Render the minimap block below the panning waveform viewport: a full-width wrapper (`touch-none select-none`, small fixed height, `var(--bg-surface)` background) containing the canvas, the window-box element, and the playhead element. Move the existing current-time / duration labels directly beneath it.
- [x] 2.3 Write a draw function that resamples `waveformPeaks` to the wrapper's device-pixel width (`bucket = floor((x / width) * bucketCount)`), clears, and fills min/max per column using the same `getComputedStyle(canvas).color` approach as the detail waveform.
- [x] 2.4 Call the draw function when `waveformPeaks` changes and on wrapper resize via a `ResizeObserver` (same pattern as the waveform viewport-width effect); keep `minimapWidthRef` updated from the same observer.
- [x] 2.5 While `isAnalyzingWaveform` is true show the same lightweight "Loading…" placeholder used by the detail waveform; if there are no peaks, render nothing in the canvas (no error).

## 3. Minimap scrubber — seek interaction

- [x] 3.1 Add `pointerdown` / `pointermove` / `pointerup` / `pointercancel` handlers on the minimap wrapper: on down, `setPointerCapture` and seek; while captured, keep seeking on move; on up/cancel, release.
- [x] 3.2 Seek maps pointer x to absolute time: `clamp((clientX - rect.left) / rect.width * duration, 0, duration)` then `handleScrub(time)`. No movement threshold — every press is a seek.
- [x] 3.3 Verify seeking does not toggle play/pause: paused stays paused and silent, playing keeps playing from the new position (reuses `handleScrub`, which already has this property).

## 4. Minimap playhead and window box

- [x] 4.1 In the existing `panWaveformTo` rAF loop, add direct style writes for the minimap playhead `left = (currentTime / duration) * minimapWidth`.
- [x] 4.2 In the same loop, position the window box: visible span seconds = `viewportWidth / WAVEFORM_PX_PER_SEC`, visible start time clamped to `[0, duration - span]`, converted to minimap px (`* minimapWidth / duration`) for `left` and `width`.
- [x] 4.3 Guard all minimap playhead / window-box writes on `duration > 0`; hide both until then (mirrors the waveform playhead's `hidden` until peaks exist).

## 5. Keyboard / assistive-technology seek

- [x] 5.1 Keep an `<input type="range" aria-label="Playback position">` bound to `currentTime` / `duration` / `handleScrub`, positioned over the minimap with the `sr-only` clip pattern (focusable and screen-reader-operable, not painted).
- [x] 5.2 Verify Tab reaches it, arrow keys move playback position, and the minimap playhead and detail waveform follow.

## 6. Grouped adjustments box

- [x] 6.1 Wrap the speed, balance, and mono controls in one container: `rounded-lg border border-[var(--border)] p-3 flex flex-col gap-3`.
- [x] 6.2 Make each row `flex items-center gap-3` with a leading `<span className="w-16 shrink-0 text-sm text-[var(--text-muted)]">` label: "Speed", "Balance", "Mono".
- [x] 6.3 Move the mono checkbox next to the "Mono" label column and drop its separate inline text; keep the balance percentage readout after the balance slider.

## 7. Verify

- [x] 7.1 `bun run lint` and `bun run build` pass.
- [x] 7.2 Manual check in the browser: load a multi-minute track; tap the far end of the minimap and confirm playback jumps there; drag across the minimap and confirm it scrubs the whole track; confirm the window box tracks the detail waveform and the playhead tracks position.
- [x] 7.3 Manual check: keyboard-only seek via the hidden range input; speed steppers move through `0.5`–`2` with the readout updating and the buttons disabling at the ends; speed/balance/mono rows are aligned inside the box.
- [x] 7.4 Manual check: a file whose waveform fails to decode still plays, with no minimap error and keyboard seek still working.
- [x] 7.5 Update the release-notes content if this project surfaces one for user-facing changes. (N/A — release notes are generated by CI from commit subjects on `v*` tag push; no in-repo content file.)

> 7.2–7.4 are interactive browser checks left for the user to run (`bun run dev`); the dev server is not started automatically per project convention.
